<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Default work day start (24h format)
    |--------------------------------------------------------------------------
    */
    'work_start' => env('ATTENDANCE_WORK_START', '08:00'),

    /*
    |--------------------------------------------------------------------------
    | Minutes after work_start still counted as "on time"
    |--------------------------------------------------------------------------
    */
    'grace_minutes' => (int) env('ATTENDANCE_GRACE_MINUTES', 15),

    /*
    |--------------------------------------------------------------------------
    | إلزام التحقق من الموقع داخل الدائرة المسموحة (للإنتاج = true)
    | للتجربة على جهاز خارج الموقع ضع false في .env
    |--------------------------------------------------------------------------
    */
    /* الافتراضي true: لا يسمح بالحضور/الانصراف إلا داخل نطاق الشركة */
    'require_geolocation' => filter_var(env('ATTENDANCE_REQUIRE_GEOLOCATION', true), FILTER_VALIDATE_BOOLEAN),

    /*
    |--------------------------------------------------------------------------
    | حد دقة الموقع المقبول بالمتر (كلما كان أقل كان أدق)
    |--------------------------------------------------------------------------
    */
    'max_location_accuracy_m' => (int) env('ATTENDANCE_MAX_LOCATION_ACCURACY_M', 300),

    /*
    |--------------------------------------------------------------------------
    | أقصى عمر مسموح لعينة الموقع بالثواني (زر موقعي الآن)
    |--------------------------------------------------------------------------
    */
    'max_location_age_seconds' => (int) env('ATTENDANCE_MAX_LOCATION_AGE_SECONDS', 90),
];
